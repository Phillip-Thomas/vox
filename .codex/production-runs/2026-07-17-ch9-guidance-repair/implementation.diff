# Replace this file with the normalized, in-scope implementation diff for the
# signed scene-contract revision. A draft-only planning run must instead state
# "no implementation authorized" and cite production-lock.md.
