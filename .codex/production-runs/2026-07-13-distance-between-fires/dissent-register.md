# Dissent Register — The Distance Between Fires

Contract revision: `intent-v1`  
Compiler review: `2026-07-14T01:44:06Z`  
Scope: all six first-wave director notes and all six recipient-authored reconciliations

## Dissent review

The compiler found two material cross-lane tensions. The frozen contract records
one causal synthesis for director ratification and one non-waivable headed
continuity gate. It does not rewrite any director note, close an objection in a
director's name, or stand in for the three independent contract signatures.

| ID | Anchor | Competing authored positions | Severity | Evidence | Decision owner | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `dissent-a4-clock-01` | `anc.a4.life-front` | Score's reconciliation removes next-bar timing authority and fixes physical life plus optional punctuation at `T0+1.750 s`. Cinematography's reconciliation holds for `1.750 s`, requests a returned next-bar time, requires first physical visibility on the immediately preceding frame, then permits optional punctuation at that later audio time. | high | `score-reconciliation.jsonl`, `cinematography-reconciliation.jsonl`, `score-treatment.md#rhythm-phrase-and-punctuation-law`, `cinematography-treatment.md#post-fx-lighting-and-render-causality-ledger` | chapter, score, and cinematography directors through exact-hash signoff | compiled synthesis awaiting signatures |
| `dissent-arrival-authority-02` | `anc.audit.arrival-handback` | Intent and both recipient reconciliations require semantic `material` authority through compliance and reserve `alive` for A4. The existing shipped baseline metadata conflicts with the authored semantic boundary, while the exact incoming pose, FOV, reality authority, life fields, and temporary effect vector remain unauthenticated until headed telemetry. No numeric camera or effect transition may be inferred from the metadata alone. | high | `shipped-visual-baseline.json`, `chapter-reconciliation.jsonl`, `cinematography-reconciliation.jsonl`, `story-intent.md#grounding` | headed continuity evidence, then all three directors | open non-waivable evidence gate |

## Resolution recorded in `intent-v1`

### A4 causal rail

The contract fixes the story event and first physical life sample at immutable
`Tfront = T0+1.750 s`, where `T0` is the committed refusal boundary. Geometry,
material, alive authority, Potato truth, reduced-motion truth, and post-off truth
all sample `Tfront`. Optional pitched bloom, emissive punctuation, or local light
may occur only at or after that first physical sample. Audio availability, a late
callback, and musical phase can neither move nor establish the front. This keeps
the causal rule accepted by both lanes: the world is real before adornment, and
sound cannot make it late.

This synthesis is frozen for review, not self-approved. Each director must sign
the exact `scene-contract.json` bytes or return a new directed note and revised
contract version.

### Arrival continuity and A4 authority

The contract preserves the semantic rule that A4 is the only material-to-alive
authority transition and forbids a cosmetic regression, FOV correction, or
restaging at audit entry. Before implementation approval, a headed real-GPU
continuity trace must authenticate the actual incoming camera owner, FOV,
reality authority, active life fields, and temporary effect owners. The contract preserves these as symbolic `auth.arrival.handback.camera` and `auth.arrival.handback.effects`; equal numeric schema fields are metadata mirrors only and license no interpolation. If the
authentic handback already committed full alive authority, implementation stops
for an upstream story-authority revision; grade or camera easing may not hide
the contradiction.

## Other cross-note resolutions

- `note-chapter-score-worker9-trace-01`: accepted. The identifiable W cell is
  licensed once beneath the hidden Maw catch and removed from oxygen pressure;
  its source stays unnamed and multiply readable.
- `note-chapter-cinematography-arrival-authority-01`: accepted as the blocking
  telemetry rule above; cinematography may freeze an authenticated state but may
  not cosmetically reconcile conflicting authority.
- `note-score-chapter-sibling-profile-001`: accepted. Tidegarden targeting must
  resolve the indivisible bundle `worldId=-1,-1:p1`, `seed=1600321158`, `profileId=story:tidegarden`, `profileVersion=1` (`story:tidegarden@1`), `profileHash=pf1-eeef3b78`, and `archetype=verdant` for every consumer. Coordinate-only and seed-only derivation are forbidden; a profile version or hash change regenerates the contract and exact-anchor evidence.
- `cin-note-chapter-a4-boundary-v1`: accepted with the measured-authority
  qualification above. Physical state precedes effect enrichment and no shipped
  arrival is replayed.

## Contract-freeze disposition

The six routed notes are losslessly compiled in `director-notes.jsonl`. No
first-wave objection remains deferred or self-closed. The A4 synthesis and the
arrival stop condition are explicit in `scene-contract.json`; final consensus
still requires three independent director signoffs over its exact SHA-256 and
headed arrival evidence remains pending by design.
