Selected direction:
Implement material-family shader detail controlled by a narrative voxel reality stage.

Implementation checklist:
- Add a story-facing reality render state independent of graphics quality.
- Add block-effect uniforms to the shared voxel shader.
- Add planet-wind uniforms sourced from `buildWindProfile`.
- Expand shader detail coverage to all `MaterialType` values.
- Add URL debug support for stage comparison.
- Add tests for stage parsing, presets, overrides, subscriptions, and material coverage.
- Validate with typecheck, full tests, build, and browser shader smoke screenshots.
