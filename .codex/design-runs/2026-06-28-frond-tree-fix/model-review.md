Pre-implementation critique:
- The current frond silhouette is failing because the renderer shows implementation scaffolding as art.
- More foliage alone would worsen the tassel look.
- The fix should make the skeleton invisible where it is only a guide and make leaves carry the silhouette.
