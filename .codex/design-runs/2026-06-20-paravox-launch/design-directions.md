# Design Directions

Depth 0 launch stabilization does not introduce thesis-distinct visual directions.

Direction A, selected: ship the efficient-collision branch game as Paravox with deployment metadata and Firebase Hosting.

Direction B, rejected for this run: create a new marketing landing page around the game. Rejected because the user asked to deploy the existing project and clarified the branch contents should be in `master`.
