Implementation handoff:

1. Capture each species close-up before patching.
2. Patch `selectLeafCandidates` first so leaves attach only to valid species supports.
3. Disable inward foliage attachment for visible cards.
4. Tune cluster density/scale per species after attachment is correct.
5. Update tests for candidate selection and attributes.
6. Re-capture every species, wide grid, mobile, and world view.
7. Run targeted tests and full verification.
