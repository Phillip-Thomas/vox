# Implementation Plan

1. Extend `TreeProfile` with shared `WindProfile`.
2. Raise canopy density range and per-silhouette leaf card budgets.
3. Make frond skeletons emit more ribs and rib segments.
4. Tune leaf cluster/card counts for fuller layered canopies.
5. Add planet wind uniforms to tree materials and copy profile values into them.
6. Update tests for deterministic wind/profile values and fuller canopy budgets.
7. Capture `tree-test.html` screenshots and in-world screenshots.
8. Run focused tests and `npm run verify`.
