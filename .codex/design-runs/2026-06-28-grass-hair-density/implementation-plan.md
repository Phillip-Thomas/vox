# Implementation Plan

1. Tune grass constants for hairlike strands: narrower base width, modest height adjustment, higher blades per density unit.
2. Replace large clump fan placement with micro-cluster placement: more roots, less obvious shared heading, smaller per-blade scatter.
3. Lower profile width multiplier range and keep density within quality-controlled bounds.
4. Add tests for hair density and width/profile invariants.
5. Run focused grass tests, then full verification if focused checks pass.
6. Capture desktop and mobile screenshots from the existing Vite preview.

