// Generation schema version. BUMP this whenever the meaning of generated data
// changes (archetype roll, resource rules, block mapping, profile shape, ...).
// Cache keys (worldGenCache) and any persisted world edits / inventory / scans
// MUST include this so stale data from an older schema is never mixed with new.
export const GENERATION_SCHEMA_VERSION = 1;
