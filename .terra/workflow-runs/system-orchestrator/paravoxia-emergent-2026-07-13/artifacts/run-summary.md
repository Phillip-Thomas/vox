# Paravoxia Emergent Batch Run Summary

The implementation candidate now carries the Story from the existing audit
arrival through Maw repair, underwater recovery, ship reconstruction, launch,
same-system travel, Tidegarden landfall, player-built shelter, and the second
hearth. The implementation includes offline movie mode and authoritative co-op,
not only planning artifacts.

Mechanical disposition:

- Main: 202 test files / 1,437 tests, typecheck, catalog, signed AV drift,
  1,035 story-authority checks, and production build passed.
- Server: 8 test files / 73 tests, typecheck, catalog, and build passed.
- Creative contract: all three directors signed the exact frozen hash; 2,320
  contract checks passed.
- Runtime: the old direct Chapter 8/9 SwiftShader renders prove canonical verdant
  identity and zero page errors, but not full scene chronology. Eight bounded
  traces overlap from the Chapter 5 transition through Chapter 7 boarding and
  from Chapter 8 crossing through dry cross-face Tidegarden traversal, natural
  nightfall, safe rest, two-world handoff, and `done`. Those ten cases plus the
  old 671.13-second full run remain diagnostic provenance only.
- Official triple: POTATO runs `v6-01`, `v6-02`, and `v6-03` passed
  `ch4-audit` → `done` in 704.18, 883.65, and 683.92 seconds against source
  fingerprint
  `a5b7dfbfad792f461a5567939fa1f4d0b2ecee205c4393c007d9edcbc9f16262`.
  Every run observed 64/64 required signed runtime anchors, completed safe rest
  and the two-world handoff, and recorded zero nudges, dry-route water-contact
  violations, runtime errors, reloads, or context losses.

The run is complete for the scoped implementation candidate. It is deliberately
not a release or publish approval. Fourteen runtime cases are retained: eleven
diagnostic cases and three official fingerprint-bound cold runs. The full
cold-run pass count is **3/3**, satisfying only that mechanical gate.

Eight release gates remain:

1. `server-issued-embodied-story-receipts`.
2. `quality-profile-matrix`.
3. `reduced-motion-equivalence`.
4. `supported-headed-input`.
5. `headed-real-gpu-taste`.
6. `origin-sibling-persistence-roundtrip`.
7. `tidegarden-support-water-authority-disposition`.
8. `human-release-decision`.

All five adapter requirements remain unbound. Until those gates close, the
public ceiling and publication lock remain unchanged.
